//Произойдет ошибка компиляции

int a = 120;
byte b = a + 10;           // Ошибка компиляции, результат выражения int
byte c = (byte)(a + 10);   // c будет равно -126 (переполнение)
byte d = a + 1;            // Ошибка компиляции, a + 1 возвращает int
